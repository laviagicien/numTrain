import { ActiveStateDirective } from './active-state.directive';

describe('ActiveStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveStateDirective();
    expect(directive).toBeTruthy();
  });
});
